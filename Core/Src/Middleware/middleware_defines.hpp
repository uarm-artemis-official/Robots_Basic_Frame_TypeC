#ifndef __MIDDLEWARE_DEFINES_HPP
#define __MIDDLEWARE_DEFINES_HPP

#include "stddef.h"

namespace MW_UART {
    constexpr size_t MAX_UART_BUFFER_SIZE =
        64;  // Maximum buffer size for UART communication
}

#endif  // __MIDDLEWARE_DEFINES_HPP